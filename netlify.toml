[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # Extended timeout for video generation (max 26 seconds on free tier)
  # Upgrade to Pro for up to 15 minutes if needed

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
